<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.murtaja.microservice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".video_stream.VideoStreaming"></activity>
        <activity android:name=".video_stream.VideoRegistration" />
        <activity android:name=".rider_apps.RidderRegister" />
        <activity android:name=".video_stream.VideoHome" />
        <activity android:name=".rider_apps.RiderHome" />
        <activity android:name=".fizza_apps.FizzaHome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>